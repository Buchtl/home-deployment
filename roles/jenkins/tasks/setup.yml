---
- name: Create /srv/jenins/data directory with specific ownership
  become: true
  file:
    path: /srv/jenkins/data
    state: directory
    owner: "{{ home_user }}"
    group: "{{ home_user | default(home_user) }}"
    mode: '0755'
